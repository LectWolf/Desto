# Architecture Decision Records

ADR 用于记录会长期约束 Desto、影响多个模块或难以撤销的技术决定。

需要 ADR 的典型问题：

- UI 与渲染技术栈。
- 桌面宿主和窗口模型。
- 配置持久化格式。
- 安装与更新机制。
- 引入大型第三方依赖。

ADR 使用递增编号，状态为 `Proposed`、`Accepted`、`Superseded` 或 `Rejected`。

新记录从 [模板](0000-template.md) 复制。
