# Desto 0.2.3.1

开发通道版本。稳定版仍显示 0.2.3。

- 设置改为系统、卡片、归档、关于四页；功能项并入系统。
- 任务栏开关文案改为返回桌面；时区跟随系统。
- 待办历史日期显示归档项；月历按数量上色。
- 展开月历时关闭添加输入，并隐藏会透出日历的标签底。
- 归档默认今天；导出按日期分组、默认近 30 天。
- 开发版 GitHub Release 标为 prerelease，不占用稳定版 latest。
