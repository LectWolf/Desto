# Desto Agent Instructions

## Communication

- 没有特殊说明时，一律使用中文回复。
- 先说明结果、约束和取舍，再展开实现细节。
- 不确定的技术判断必须通过原型、测试或测量验证，不能直接固化为架构事实。
- 每次最终回复必须说明“你需要提供什么”和“下一步做什么”。
- 每次面向用户的阶段更新和最终回复必须以 `（N/总数）事项名` 标明 [交付路线](docs/ROADMAP.md) 中的当前进度。
- 路线事项按一次可完整验收的粒度拆分；默认一个事项一次处理完，不得按回复次数或提交数量递增。
- 只有验收未通过、出现新的底层缺口、用户新增范围或需要真实 Windows/硬件验证时，才在同一编号继续处理。
- 用户新增功能先插入最早的依赖位置，重新计算总数并在回复中说明；临时插入任务沿用其所属事项编号。
- 只有确实需要用户选择、确认或提供外部信息时才提出请求；没有需要时明确写“你无需提供内容”。
- “下一步”必须描述准备继续交付的具体内容，不能只写笼统的继续开发。

## Project Direction

- Desto 是面向 Windows 的轻量桌面整理工具。
- 核心目标是稳定、快速、低资源占用、简约且精致。
- 功能数量不是进度指标；完整、可预测的核心体验优先于功能堆叠。
- 功能能力、界面入口和视觉表现是三个独立维度，不得互相绑死。
- 对用户有实际意义的功能入口和外观，应允许按收纳盒实例单独配置。
- 全局设置只提供默认值，不能覆盖用户已经明确设置的实例偏好。

## Architecture

- 先维护稳定的项目骨架，再在骨架内交付原子化能力。
- 模块应有清晰职责、小接口和足够深的实现；避免大量只做转发的浅模块。
- `Card` 是所有卡片类型共享的基础模型，负责身份、生命周期、能力和实例配置；位置和尺寸由 `WorkspaceLayout` 的 Placement 管理。
- 具体卡片类型在 `Card` 之上扩展内容与行为，不重复实现通用窗口、布局和外观逻辑。
- 可组合行为优先于不断加深继承层级；只有确实存在多种实现时才建立可替换接缝。
- 模块之间通过明确的数据、命令和事件交互，不依赖隐式全局状态。
- 操作系统、存储、渲染和框架细节必须集中在适配位置，不能渗透领域模型。
- 跨模块、难以撤销或会长期约束项目的决定必须记录 ADR。

## Changes

- 每次改动只解决一个明确问题，或交付一个能够独立验证的完整能力。
- 一个能力应包含正常路径、失败处理、验证和必要文档。
- 不在功能改动中夹带无关重构，也不为假想需求提前增加抽象。
- 修改稳定接口前，先说明调用方影响、数据兼容性和迁移方案。
- 发现现有改动时应与其协作，不得擅自覆盖或回退用户工作。

## Performance

- 内存、启动时间、显隐延迟、空闲 CPU 和交互响应属于产品行为。
- 性能判断必须基于可重复的基准场景和改动前后数据。
- 新功能应说明常驻成本、按需成本和释放时机。
- 优化不能以破坏正确性、数据安全或交互一致性为代价。
- 视觉效果和功能入口可以由用户按实例取舍，但内部资源必须在关闭后真正停止使用。

## Quality

- 测试优先覆盖稳定模块接口和用户可观察行为。
- Bug 修复前先建立能够识别原问题的反馈信号。
- 核心算法应尽可能与窗口、文件系统、线程和操作系统调用分离。
- 配置和布局必须支持版本迁移、安全写入与异常恢复。
- 错误不得静默损坏数据；面向用户的信息保持简洁，诊断信息进入日志。
- 完成任务前运行与风险相匹配的测试、静态检查和性能检查。

## Dependencies

- 优先使用操作系统、标准库和已选框架现有能力。
- 新依赖必须说明必要性、维护状态、许可证、发布体积和运行时成本。
- 不因局部开发方便引入大型运行时或让依赖类型扩散到整个项目。
- UI 技术栈在原型验证和 ADR 通过前保持开放，不默认锁定 Qt 或纯 Win32。

## Documentation

- 产品边界记录在 `docs/PRODUCT.md`。
- 模块结构和稳定接口记录在 `docs/ARCHITECTURE.md`。
- 性能场景和预算记录在 `docs/PERFORMANCE.md`。
- 配置层级和迁移原则记录在 `docs/CONFIGURATION.md`。
- 整体阶段、当前进度和退出条件记录在 `docs/ROADMAP.md`。
- 文档描述长期规则；具体功能行为、阈值和验收条件放在功能文档、Issue 或测试中。
